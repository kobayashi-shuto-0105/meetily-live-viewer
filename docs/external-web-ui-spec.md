## 外部のwebUI

- 作成するwebUIで文字起こしの内容等の修正(編集)、コメント追加、ハイライト等ができれば良いなと思っているので、Meetilyが現状保存している文字起こしを生データとして、それとは別でデータ(コメントや文字起こし修正後のデータ)を持てる必要がある、
- websocketでwebUIがリアルタイムに更新されて外部からブラウザで見れる
- 新規webUIは`frontend/` 内ではなく、`ext-frontend/` につくりたい、
